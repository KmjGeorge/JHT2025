import numpy as np


def missing_pdw(missing_rate):
    pass

def interleaving(pdw_trains, mix_num, missing_uprate=30):
    '''

    Args:
        pdw_trains: 由PDW类组成的列表（每个PDW类包含若干个PDW）
        mix_num: 混合数量
        missing_rate: 漏脉冲率的上限
    Returns:

    '''
    # selected = np.random.choice(pdw_trains, mix_num, replace=False)
    # for pdw_train in selected:
    #     pdw_train =

